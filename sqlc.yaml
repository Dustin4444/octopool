version: "2"
sql:
  - engine: "sqlite"
    schema:
      - "migrations"
      - "sql/schema"
    queries: "sql/queries"
    gen:
      go:
        package: "sqlvalidate"
        out: ".sqlc-validation"
        emit_prepared_queries: false
        emit_interface: false
